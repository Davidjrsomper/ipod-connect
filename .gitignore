.build/
build/
.tools/
releases/*.zip
releases/old_updates/
*.xcodeproj
.DS_Store
.swiftpm/
keys/

# Build artifacts — these go to GitHub Releases, not into git history
dist/
releases/*.zip
releases/*.html

# Raw screenshots. The optimised *-web.jpg files are what the site embeds;
# these originals are several MB each and easy to retake.
web/img/*.png
